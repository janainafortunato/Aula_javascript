//retorna o tamnho de uma string
const textSize = 'texto';
const textSize = 'texto'.length;
console.log(`Quantidade de letras: ` ${textSize});

//Retorna um array quebrado a string po um delimitador
const splittedText = 'Texto'.split('x');
console.log('\nArray com as possições separadas pelo delimitador:', splittedText);